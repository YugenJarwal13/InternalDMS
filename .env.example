# Database Configuration
DATABASE_URL=postgresql://username:password@localhost/dbname
# For SQLite (development): DATABASE_URL=sqlite:///./test.db

# Storage Configuration
STORAGE_PATH=/path/to/your/storage/directory
# For relative path (default): storage
# For Docker: /app/storage
# For Windows: C:\path\to\storage
# For Linux/Mac: /var/lib/dms/storage

# JWT Configuration (if needed in future)
# SECRET_KEY=your-secret-key
# ACCESS_TOKEN_EXPIRE_MINUTES=480
